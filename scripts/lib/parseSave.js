// import { Folder } from "../model";

// export const parseSave = (save) => {
//     const root = new Folder("root", [], []);
//     let currentFolders = root.folders;
//     while(currentFolders.length !== 0){
//         currentFolders.forEach((folder) => {
//             root.addFolder(new Folder())
//         });
//     }
// };